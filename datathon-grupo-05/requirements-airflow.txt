# Dependências leves para as tasks do pipeline Airflow
# Exclui deliberadamente: PyTorch CUDA, stack LLM/agente, serving, dev
# torch é instalado separado no Dockerfile.airflow com wheel CPU-only (~200MB vs ~3GB)
pandas>=2.2.1
numpy>=1.26.0
scikit-learn>=1.4.1
pandera>=0.18.3
yfinance>=0.2.37
mlflow>=2.11.1
evidently>=0.4.17
